# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PROCH' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PROCH
pod 'GooglePlaces'
pod 'GooglePlacePicker'
pod 'GoogleMaps'
pod 'SwiftLint'
pod 'Alamofire', '~>4.4'
pod 'Firebase/Core'
pod 'ApiAI'

  target 'PROCHTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PROCHUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
